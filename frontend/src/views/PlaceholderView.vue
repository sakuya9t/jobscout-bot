<template>
  <section class="panel">
    <div class="empty">
      This panel isn’t in the new interface yet — it’s still available on the
      <a href="/">classic dashboard</a>.
    </div>
  </section>
</template>

<style scoped>
.empty {
  border: 1px dashed var(--line);
  border-radius: 8px;
  padding: 28px;
  color: var(--muted);
  background: var(--surface-raised);
  text-align: center;
}
</style>
