# Runtime dependencies for Vercel's Python runtime (installs from requirements.txt,
# not pyproject.toml). Keep in sync with [project].dependencies in pyproject.toml.
# The `browser` (playwright) and `dev` (pytest) extras are intentionally excluded.
fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
pydantic>=2.6
pydantic-settings>=2.2
email-validator>=2
python-multipart>=0.0.9
bcrypt>=4.1
PyJWT>=2.8
httpx>=0.27
# Browser-TLS-impersonating HTTP client, used to read boards that block plain httpx
# via TLS/JA3 fingerprinting (e.g. Citadel's Cloudflare-fronted detail pages). Ships
# manylinux wheels, so it installs on Vercel / GitHub Actions without a browser.
curl_cffi>=0.7
tenacity>=8.2
beautifulsoup4>=4.12
lxml>=5.2
pypdf>=4.2
python-docx>=1.1
apscheduler>=3.10
jinja2>=3.1
mcp>=1.2
# Postgres driver — JOBSCOUT_DATABASE_URL points at Supabase Postgres in prod.
psycopg2-binary>=2.9
